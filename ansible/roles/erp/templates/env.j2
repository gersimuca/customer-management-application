DB_JDBC_URL={{ datasource.jdbc.url }}
DB_USERNAME={{ datasource.credentials.username }}
DB_PASSWORD={{ datasource.credentials.password }}
DB_JDBC_DRIVER={{ datasource.jdbc.driver }}

SSO_JWK_SET_URI={{ security.oauth2.resourceserver.jwt.jwk_set_uri }}

CORS_ALLOWED_ORIGINS={{ cors.allowed_origins | join(',') }}

LOG_LEVEL={{ logging.level.root }}

SSO_CLIENT_ID={{ sso.client_id }}

NAGERDATE_CLIENT_ID={{ nagerdate.client_id }}
NAGERDATE_AUTHORIZATION_GRANT_TYPE={{ nagerdate.authorization_grant_type }}
NAGERDATE_PROVIDER={{ nagerdate.provider }}
NAGERDATE_SSO_JWT_TOKEN_URI={{ nagerdate.token_uri }}

NAGERDATE_API_URL={{ nagerdate.api.url }}
NAGERDATE_USERNAME={{ nagerdate.credentials.username }}
NAGERDATE_PASSWORD={{ nagerdate.credentials.password }}

FLYWAY_LOCATION={{ flyway.locations | join(',') }}

REDIS_KEY_PREFIX={{ redis.cache.key_prefix }}
REDIS_USERNAME={{ redis.credentials.username }}
REDIS_PASSWORD={{ redis.credentials.password }}